class Posts {
  String image, description, date, time, name;

  Posts(
      {required this.image,
      required this.description,
      required this.date,
      required this.time,
      required this.name});
}
